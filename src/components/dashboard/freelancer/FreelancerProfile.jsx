import React from 'react';
import RoleProfile from '../common/RoleProfile';

const FreelancerProfile = (props) => (
  <RoleProfile {...props} role="freelancer" />
);

export default FreelancerProfile;
